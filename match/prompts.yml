matches:
# ===== SUMMARIZATION =====
  - triggers: [";prsummarize ", ";summarize ", ";summary "]
    replace: |
      Summarize this text in bullet points. Respond back in a markdown code block:

  - triggers: [";prsummarize2 ", ";summarize2 ", ";summary2 "]
    replace: |
      Summarize this text in bullet points:

# ===== ANALYSIS & COMPARISON =====
  - triggers: [";prcompare ", ";compare "]
    replace: |
      List the pros and cons of:

  - triggers: [";prcompare2 ", ";compare2 "]
    replace: |
      Compare these options and list pros/cons for each:

# ===== TEXT IMPROVEMENTS =====
  - triggers: [";prfix ", ";fixtypos "]
    replace: |
      Fix typos and grammar in this text:

  - triggers: [";prrewrite ", ";rewrite "]
    replace: |
      Rewrite this text to sound better:

  - triggers: [";prshorten ", ";shorten "]
    replace: |
      Shorten this text while keeping the key points:

  - triggers: [";prexpand ", ";expand "]
    replace: |
      Expand this text with more details and examples:

# ===== LEARNING =====
  - triggers: [";prexplain ", ";explain "]
    replace: |
      Explain this concept in simple terms with examples and use cases:

# ===== CODE =====
  - triggers: [";prcodereview ", ";codereview "]
    replace: |
      Review this code for bugs, performance issues, and best practices:

  - triggers: [";prrefactor "]
    replace: |
      Suggest refactoring improvements for this code:
