matches:
# ===== SUMMARIZATION =====
  - triggers: [";ppsummarize ", ";summarize ", ";summary ", ";sum "]
    replace: |-
      Summarize this text in bullet points. Respond back in a markdown code block:

      """
      $|$
      """
    force_mode: clipboard
  - triggers: [";ppsummarizen ", ";summarizen ", ";summaryn ", ";sumn "]
    replace: |-
      Summarize this text in bullet points:

      """
      $|$
      """
    force_mode: clipboard
# ===== ANALYSIS & COMPARISON =====
  - triggers: [";ppcompare ", ";compare "]
    replace: |-
      List the pros and cons of:

      """
      $|$
      """
    force_mode: clipboard
  - triggers: [";ppcompare2 ", ";compare2 "]
    replace: |-
      Compare these options and list pros/cons for each:

      """
      $|$
      """
    force_mode: clipboard
# ===== TEXT IMPROVEMENTS =====
  - triggers: [";ppfix ", ";fixtypos ", ";fixgrammar ", ";fixt ", ";fixg "]
    replace: |-
      Fix typos and grammar in this text. Respond back in a markdown code block:

      """
      $|$
      """
    force_mode: clipboard
  - triggers: [";ppfixn ", ";fixtyposn ", ";fixgrammarn ", ";fixtn ", ";fixgn "]
    replace: |-
      Fix typos and grammar in this text:

      """
      $|$
      """
    force_mode: clipboard
  - triggers: [";pprewrite ", ";rewrite ", ";rew ", ";ref "]
    replace: |-
      Rewrite this text to sound better. Respond back in a markdown code block:

      """
      $|$
      """
    force_mode: clipboard
  - triggers: [";pprewriten ", ";rewriten ", ";rewn ", ";refn "]
    replace: |-
      Rewrite this text to sound better:

      """
      $|$
      """
    force_mode: clipboard
  - triggers: [";ppshorten ", ";shorten "]
    replace: |-
      Shorten this text while keeping the key points. Respond back in a markdown code block:

      """
      $|$
      """
    force_mode: clipboard
  - triggers: [";ppshortenn ", ";shortenn "]
    replace: |-
      Shorten this text while keeping the key points:

      """
      $|$
      """
    force_mode: clipboard
  - triggers: [";ppexpand ", ";expand "]
    replace: |-
      Expand this text with more details and examples. Respond back in a markdown code block:

      """
      $|$
      """
    force_mode: clipboard
  - triggers: [";ppexpandn ", ";expandn "]
    replace: |-
      Expand this text with more details and examples:

      """
      $|$
      """
    force_mode: clipboard
# ===== LEARNING =====
  - triggers: [";ppexplain ", ";explain "]
    replace: |-
      Explain this concept in simple terms with examples and use cases:

      """
      $|$
      """
    force_mode: clipboard
# ===== CODE =====
  - triggers: [";ppcodereview ", ";codereview "]
    replace: |-
      Review this code for bugs, performance issues, and best practices:

      ```
      $|$
      ```
    force_mode: clipboard
  - triggers: [";pprefactor ", ";refactorcode "]
    replace: |-
      Suggest refactoring improvements for this code:

      ```
      $|$
      ```
    force_mode: clipboard
